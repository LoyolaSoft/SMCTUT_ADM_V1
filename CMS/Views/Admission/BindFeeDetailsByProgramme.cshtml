@model CMS.ViewModel.ViewModel.AdmissionViewModel
@{int count = 0;}
@if (Model.liFeeStructure != null)
{
    <table class="table table-hover table-bordered">
        <thead>
            <tr>
                <th>#</th>
                <th>Frequency</th>
                <th>Main Head</th>
                <th>Amount</th>
                <th>Fee Category</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.liFeeStructure)
            {
                //if (Model.liFeeStructure != null)
                //{
                //var FrequecyList = Model.liFeeStructure.Where(s => s.FREQUENCY_ID == item.FREQUENCY_ID).ToList();
                //if (FrequecyList.Count > 0)
                //{
                <tr>
                    <td>
                        @{ count++;}@count
                    </td>
                    <td>@item.FREQUENCY_NAME</td>
                    <td>@item.MAIN_HEAD</td>
                    <td>@item.AMOUNT</td>
                    <td>@item.FEE_CATEGORY</td>
                </tr>
                                        }
                                    
                                
        </tbody>
    </table>
                            }